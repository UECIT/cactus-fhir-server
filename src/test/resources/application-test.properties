# database set up
spring.datasource.url=jdbc:h2:mem:test
spring.jpa.properties.hibernatedialect=org.hibernate.dialect.H2Dialect

fhir.server=http://localhost:${server.port}/fhir
server.host=http://localhost:${server.port}